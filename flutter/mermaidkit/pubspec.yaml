name: mermaidkit
description: Native Mermaid diagram rendering for Flutter — a SceneWire scene to a Skia CustomPainter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
